/*window.onload = function() {
  var contractname = localStorage.getItem("contractname");
  db.contracts.find({ contractname: { $eq: contractname } })
  window.alert(db.contracts.find({ contractname: { $eq: contractname } }))
}